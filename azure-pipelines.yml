# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- scripts: |
    docker build -t damienpond/azure_test .
    docker login -u damienpond -p $(DOCKER_PW)
    docker push damienpond/azure_test
